<!DOCTYPE html>
<html lang="ko">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>OrcaX 농장 입구</title>
  <script src="https://developers.kakao.com/sdk/js/kakao.js"></script>
  <style>
    body {
      margin: 0;
      padding: 0;
      background: url('farm-000.png') no-repeat center center fixed;
      background-size: cover;
      color: #fff;
      font-family: 'Segoe UI', Tahoma, Geneva, Verdana, sans-serif;
      text-align: center;
    }
    .container {
      padding: 3rem;
    }
    h1 {
      font-size: 2rem;
      margin-bottom: 1rem;
      background-color: rgba(0, 0, 0, 0.5);
      display: inline-block;
      padding: 0.5rem 1rem;
      border-radius: 10px;
    }
    .nickname {
      font-size: 1rem;
      margin-bottom: 2rem;
      background-color: rgba(0, 0, 0, 0.4);
      display: inline-block;
      padding: 0.3rem 0.8rem;
      border-radius: 8px;
      color: #fff;
    }
    .choice-section {
      display: flex;
      justify-content: center;
      align-items: flex-end;
      gap: 5rem;
      margin-top: 2rem;
      flex-wrap: wrap;
    }
    .choice-item {
      margin-bottom: 2rem;
    }
    .choice-item img {
      width: 180px;
      height: auto;
      border-radius: 12px;
      cursor: pointer;
      transition: transform 0.2s;
      box-shadow: 0 0 10px #000;
    }
    .choice-item img:hover {
      animation: squishy 0.4s ease-in-out;
    }
    @keyframes squishy {
      0% { transform: scale(1.05, 0.95); }
      50% { transform: scale(0.95, 1.05); }
      100% { transform: scale(1.05, 0.95); }
    }
    .choice-item span {
      display: block;
      margin-top: 0.5rem;
      font-weight: bold;
      background-color: rgba(0, 0, 0, 0.6);
      padding: 0.2rem 0.5rem;
      border-radius: 5px;
    }
    .exit-link {
      position: absolute;
      bottom: 20px;
      left: 50%;
      transform: translateX(-50%);
      font-size: 0.85rem;
      color: #eee;
      text-decoration: underline;
      cursor: pointer;
      background-color: rgba(0, 0, 0, 0.5);
      padding: 0.3rem 0.8rem;
      border-radius: 5px;
    }
    .exit-link:hover {
      color: #fff;
    }
    .kakao-login {
      margin-top: 3rem;
    }
    .kakao-btn {
      background: #FEE500;
      color: #000;
      border: none;
      padding: 12px 24px;
      border-radius: 6px;
      font-size: 1rem;
      cursor: pointer;
    }
    .market-link {
      position: fixed;
      bottom: 20px;
      right: 20px;
      background-color: rgba(255, 255, 255, 0.9);
      color: #222;
      padding: 10px 16px;
      border-radius: 6px;
      font-size: 0.9rem;
      text-decoration: none;
      font-weight: bold;
      box-shadow: 0 0 8px rgba(0,0,0,0.3);
    }
    .market-link:hover {
      background-color: #fff;
    }

    @media (max-width: 768px) {
      .container {
        padding: 1.5rem;
      }
      h1 {
        font-size: 1.5rem;
      }
      .choice-section {
        flex-direction: column;
        gap: 2rem;
      }
      .choice-item img {
        width: 150px;
      }
      .market-link {
        font-size: 0.8rem;
        padding: 8px 12px;
      }
    }
  </style>
</head>
<body>
  <div class="container">
    <h1>OrcaX Sea Farm에 오신 것을 환영합니다.</h1>
    <div id="nicknameDisplay" class="nickname"></div>

    <div class="choice-section">
      <a href="gamja-farm.html" class="choice-item">
        <img src="farm-101.png" alt="감자 밭 입장">
        <span>감자 밭</span>
      </a>
      <a href="bori-farm.html" class="choice-item">
        <img src="farm-201.png" alt="보리 밭 입장">
        <span>보리 밭</span>
      </a>
    </div>

    <div class="kakao-login">
      <button class="kakao-btn" onclick="goKakaoLogin()">카카오 간편 로그인</button>
    </div>
  </div>

  <a class="exit-link" href="index.html">⬅ 농장 밖으로 나가기</a>
  <a class="market-link" href="auction-house.html">📊 경매장 바로가기</a>

  <script>
    Kakao.init('284b798a9d9be8202f9c2e129fa6f329');
    function goKakaoLogin() {
      const client_id = "284b798a9d9be8202f9c2e129fa6f329";
      const redirect_uri = "https://byungil-cho.github.io/OrcaX/kakao-callback.html";
      const url = `https://kauth.kakao.com/oauth/authorize?response_type=code&client_id=${client_id}&redirect_uri=${encodeURIComponent(redirect_uri)}`;
      window.location.href = url;
    }

    const nickname = localStorage.getItem("nickname");
    if (nickname) {
      document.getElementById("nicknameDisplay").textContent = `👤 ${nickname}님, 환영합니다!`;
    }
  </script>
</body>
</html>
